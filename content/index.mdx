# Welcome

This is your documentation site, built with [Nextra](https://nextra.site).

## Getting started

Edit `content/index.mdx` to update this page. Add more `.mdx` files under `content/` to grow your docs.

## Local development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to preview.
