# Coordinator & AI views

Optional v1 surfaces for the Coordinator agent and AI View Assistant.

> **Base:** `/api/v1`  
> **Auth:** `Authorization: Bearer {token}`  
> **Status:** Planned (coming soon)
> These endpoints are session-oriented and may stream.

---

## Endpoints

| Method | Path | Action |
| --- | --- | --- |
| `GET` | [`/coordinator/chats`](#get-coordinatorchats) | List chats |
| `POST` | [`/coordinator/chats`](#post-coordinatorchats) | Create chat |
| `GET` | [`/coordinator/chats/{chatId}`](#get-coordinatorchatschatid) | Chat detail |
| `POST` | [`/coordinator/chats/{chatId}/messages`](#post-coordinatorchatschatidmessages) | Send message (stream optional) |
| `DELETE` | [`/coordinator/chats/{chatId}`](#delete-coordinatorchatschatid) | Delete chat |
| `GET` | [`/warehouses/{warehouseId}/view-chats`](#get-warehouseswarehouseidview-chats) | List AI view chats |
| `POST` | [`/warehouses/{warehouseId}/view-chats`](#post-warehouseswarehouseidview-chats) | Create view chat |
| `GET` | [`/warehouses/{warehouseId}/view-chats/{chatId}`](#get-warehouseswarehouseidview-chatschatid) | View chat detail |
| `POST` | [`/warehouses/{warehouseId}/view-chats/{chatId}/message`](#post-warehouseswarehouseidview-chatschatidmessage) | Query / draft via chat |
| `DELETE` | [`/warehouses/{warehouseId}/view-chats/{chatId}`](#delete-warehouseswarehouseidview-chatschatid) | Delete view chat |
| `POST` | [`/warehouses/{warehouseId}/schema-index`](#post-warehouseswarehouseidschema-index) | Index table for AI |
| `DELETE` | [`/warehouses/{warehouseId}/schema-index/{tableName}`](#delete-warehouseswarehouseidschema-indextablename) | Remove index |

## Coordinator chats

Legacy prefix: `/api/coordinator-chats`  
v1 prefix: `/coordinator/chats`

### Shared types

#### `CoordinatorChatSummary`

| Field | Type | Description |
| --- | --- | --- |
| `chat_id` | `string` | UUID |
| `title` | `string \| null` | |
| `created_at` | `string` | ISO 8601 |
| `updated_at` | `string \| null` | ISO 8601 |

#### `CoordinatorMessageRequest`

| Field | Type | Description |
| --- | --- | --- |
| `message` | `string` | User message |
| `context` | `object` | Optional structured context (warehouse, plan, etc.) |

---

### `GET /coordinator/chats`

List coordinator chats for the current user.

| | |
| --- | --- |
| **Status** | Planned (today: `GET /api/coordinator-chats`) |
| **Scopes** | `coordinator:read` |

#### Response `200 OK`

```json
{
  "chats": [
    {
      "chat_id": "c0ffee00-…",
      "title": "Set up SFTP download",
      "created_at": "2026-07-10T09:00:00Z",
      "updated_at": "2026-07-11T11:20:00Z"
    }
  ]
}
```

---

### `POST /coordinator/chats`

Create a new coordinator chat.

| | |
| --- | --- |
| **Status** | Planned (today: `POST /api/coordinator-chats`) |
| **Scopes** | `coordinator:write` |

#### Request body

```json
{}
```

#### Response `201 Created` / `200 OK`

```json
{
  "chat_id": "c0ffee00-…"
}
```

---

### `GET /coordinator/chats/{chatId}`

Chat detail including message history.

| | |
| --- | --- |
| **Status** | Planned (today: `GET /api/coordinator-chats/{chatId}`) |
| **Scopes** | `coordinator:read` |

#### Response `200 OK`

```json
{
  "chat_id": "c0ffee00-…",
  "messages": [
    { "role": "user", "content": "Help me connect an SFTP credential.", "timestamp": "…" },
    { "role": "assistant", "content": "Let's start with the host and username.", "timestamp": "…" }
  ]
}
```

---

### `POST /coordinator/chats/{chatId}/messages`

Send a message (non-stream or buffered response).

| | |
| --- | --- |
| **Status** | Planned (today: `POST /api/coordinator-chats/{chatId}` (legacy posts to chat root)) |
| **Scopes** | `coordinator:write` |

#### Request body

```json
{
  "message": "Create a credential for our vendor SFTP and a download job."
}
```

#### Response `200 OK`

```json
{
  "assistant_message": "…",
  "plan": { },
  "actions": []
}
```

> Response shape is assistant-defined and may include plan panes, credential wizards, or action proposals. Treat unknown fields as additive.

v1 path uses `/messages` for clarity; legacy posts to `POST /api/coordinator-chats/{chatId}`.

---

### `POST /coordinator/chats/{chatId}/messages` (stream)

Stream assistant tokens / events.

| | |
| --- | --- |
| **Status** | Planned (today: `POST /api/coordinator-chats/{chatId}/stream`) |
| **Scopes** | `coordinator:write` |
| **Accept** | Prefer `text/event-stream` |

#### Request body

Same as non-stream message request.

#### Response `200 OK` — SSE

```
event: token
data: {"text":"I'll"}

event: token
data: {"text":" help"}

event: done
data: {"chat_id":"c0ffee00-…"}
```

Exact event names may vary; clients should ignore unknown events.

```bash
curl -N -X POST \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -d '{"message":"List my warehouses"}' \
  "https://{tenant}.ryanplatform.com/api/v1/coordinator/chats/$CHAT_ID/messages?stream=true"
```

Legacy stream URL: `POST /api/coordinator-chats/{chatId}/stream`.

---

### `DELETE /coordinator/chats/{chatId}`

Delete a coordinator chat.

| | |
| --- | --- |
| **Status** | Planned (today: `DELETE /api/coordinator-chats/{chatId}`) |
| **Scopes** | `coordinator:write` |

#### Response `200 OK` / `204 No Content`

---

## AI view chats

Legacy / current prefix: `/api/warehouses/{warehouseId}/view-chats`  
v1 keeps the warehouse-scoped path.

### `GET /warehouses/{warehouseId}/view-chats`

List AI view chats for a warehouse.

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:read` |

#### Response `200 OK`

```json
{
  "chats": [
    {
      "chat_id": "viewchat-…",
      "title": "Recon breaks view",
      "message_count": 4,
      "updated_at": "2026-07-11T10:00:00Z"
    }
  ]
}
```

---

### `POST /warehouses/{warehouseId}/view-chats`

Create a view chat.

| | |
| --- | --- |
| **Status** | Planned (today: `POST /api/warehouses/{warehouseId}/view-chats`) |
| **Scopes** | `ai-views:write` |

#### Request body

```json
{}
```

#### Response `200 OK`

```json
{
  "chat_id": "viewchat-…"
}
```

---

### `GET /warehouses/{warehouseId}/view-chats/{chatId}`

Chat detail (messages + draft view definition when present).

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:read` |

---

### `POST /warehouses/{warehouseId}/view-chats/{chatId}/message`

Send a message to draft or refine a warehouse view.

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:write` |

#### Request body

```json
{
  "message": "Join Positions to Prices on Symbol and filter AsOf to today."
}
```

#### Response `200 OK`

```json
{
  "assistant_message": "Draft view updated with an INNER join and AsOf filter.",
  "draft_view": {
    "name": "v_positions_with_prices",
    "base_table": "dbo.Positions",
    "joins": [],
    "columns": [],
    "where": []
  }
}
```

```bash
curl -sS -X POST \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message":"Join Positions to Prices on Symbol"}' \
  "https://{tenant}.ryanplatform.com/api/v1/warehouses/7/view-chats/$CHAT_ID/message"
```

---

### `DELETE /warehouses/{warehouseId}/view-chats/{chatId}`

Delete a view chat.

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:write` |

---

## Schema index (AI)

Index warehouse tables so the assistant can reason about schema.

Legacy prefix: `/api/warehouses/{warehouseId}/schema-index`

### `POST /warehouses/{warehouseId}/schema-index`

Index one or more tables for AI retrieval.

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:write` |

#### Request body

```json
{
  "table_name": "dbo.Positions"
}
```

or

```json
{
  "table_names": ["dbo.Positions", "dbo.Prices"]
}
```

#### Response `200 OK`

```json
{
  "indexed": ["dbo.Positions"],
  "status": "ready"
}
```

---

### `DELETE /warehouses/{warehouseId}/schema-index/{tableName}`

Remove a table from the AI schema index.

| | |
| --- | --- |
| **Status** | Planned |
| **Scopes** | `ai-views:write` |

#### Path parameters

| Name | Type | Description |
| --- | --- | --- |
| `tableName` | `string` | URL-encoded table name |

#### Response `200 OK` / `204 No Content`

```bash
curl -sS -X DELETE \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  "https://{tenant}.ryanplatform.com/api/v1/warehouses/7/schema-index/dbo.Positions"
```

---

