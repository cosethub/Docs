import { Callout } from 'nextra/components'

# XSL Transform

Transform XML with an XSLT stylesheet.

| | |
| --- | --- |
| **Category** | Systems and Security |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Provide XML input and XSLT.
2. Write transformed output.
3. Use in XML-heavy integrations.

<Callout type="info">
  **Screenshot placeholder** (`job-xsl-transform-picker.png`)

  Placeholder: Job type picker with XSL Transform selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-xsl-transform-config.png`)

  Placeholder: Configure Job Fields for XSL Transform.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
