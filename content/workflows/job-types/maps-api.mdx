import { Callout } from 'nextra/components'

# MAPS API

Call MAPS API for alternative investment data.

| | |
| --- | --- |
| **Category** | Finance and Accounting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Configure API credentials.
2. Request data.
3. Save or load outputs.

<Callout type="info">
  **Screenshot placeholder** (`job-maps-api-picker.png`)

  Placeholder: Job type picker with MAPS API selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-maps-api-config.png`)

  Placeholder: Configure Job Fields for MAPS API.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
