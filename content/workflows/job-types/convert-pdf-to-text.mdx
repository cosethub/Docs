import { Callout } from 'nextra/components'

# Convert PDF to Text

Extract text from PDF files (optionally selected pages).

| | |
| --- | --- |
| **Category** | Systems and Security |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Select PDF source.
2. Optionally limit pages.
3. Save text output.

<Callout type="info">
  **Screenshot placeholder** (`job-convert-pdf-to-text-picker.png`)

  Placeholder: Job type picker with Convert PDF to Text selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-convert-pdf-to-text-config.png`)

  Placeholder: Configure Job Fields for Convert PDF to Text.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
