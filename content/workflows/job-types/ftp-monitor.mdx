import { Callout } from 'nextra/components'

# FTP Monitor

Watch an FTP/SFTP site until a matching file appears (or timeout).

| | |
| --- | --- |
| **Category** | Email and Communication |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Point at FTP credentials and remote folder.
2. Define file name / pattern and wait window.
3. On success, usually chain FTP Download.

<Callout type="info">
  **Screenshot placeholder** (`job-ftp-monitor-picker.png`)

  Placeholder: Job type picker with FTP Monitor selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-ftp-monitor-config.png`)

  Placeholder: Configure Job Fields for FTP Monitor.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
