import { Callout } from 'nextra/components'

# SSRS Report Run

Run an SSRS report and save it to a folder in the chosen format.

| | |
| --- | --- |
| **Category** | Database and Reporting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Pick report and parameters.
2. Choose output format and path.
3. Email or transmit the file next.

<Callout type="info">
  **Screenshot placeholder** (`job-ssrs-report-run-picker.png`)

  Placeholder: Job type picker with SSRS Report Run selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-ssrs-report-run-config.png`)

  Placeholder: Configure Job Fields for SSRS Report Run.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
