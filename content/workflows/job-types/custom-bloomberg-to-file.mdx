import { Callout } from 'nextra/components'

# Custom Bloomberg to File

Run custom Bloomberg pricing queries and export formatted files.

| | |
| --- | --- |
| **Category** | Finance and Accounting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Define custom pricing query.
2. Export to destination files.
3. Feed warehouse or recon jobs.

<Callout type="info">
  **Screenshot placeholder** (`job-custom-bloomberg-to-file-picker.png`)

  Placeholder: Job type picker with Custom Bloomberg to File selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-custom-bloomberg-to-file-config.png`)

  Placeholder: Configure Job Fields for Custom Bloomberg to File.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
