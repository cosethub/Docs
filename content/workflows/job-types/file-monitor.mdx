import { Callout } from 'nextra/components'

# File Monitor

Watch a local directory until matching files arrive (or timeout).

| | |
| --- | --- |
| **Category** | Systems and Security |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Set folder and file pattern.
2. Set wait window.
3. On success, process the file.

<Callout type="info">
  **Screenshot placeholder** (`job-file-monitor-picker.png`)

  Placeholder: Job type picker with File Monitor selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-file-monitor-config.png`)

  Placeholder: Configure Job Fields for File Monitor.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
