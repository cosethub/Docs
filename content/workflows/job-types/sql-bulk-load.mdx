import { Callout } from 'nextra/components'

# SQL Bulk Load

Load a CSV into a SQL table (create or append).

| | |
| --- | --- |
| **Category** | Database and Reporting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Pick database and target table.
2. Point at the CSV.
3. Run after Excel→CSV or downloads.

<Callout type="info">
  **Screenshot placeholder** (`job-sql-bulk-load-picker.png`)

  Placeholder: Job type picker with SQL Bulk Load selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-sql-bulk-load-config.png`)

  Placeholder: Configure Job Fields for SQL Bulk Load.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
