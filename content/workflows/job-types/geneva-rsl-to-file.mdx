import { Callout } from 'nextra/components'

# Geneva RSL to File

Run an RSL report and save it to a directory.

| | |
| --- | --- |
| **Category** | Finance and Accounting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Select RSL and parameters.
2. Choose output directory.
3. Distribute or load next.

<Callout type="info">
  **Screenshot placeholder** (`job-geneva-rsl-to-file-picker.png`)

  Placeholder: Job type picker with Geneva RSL to File selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-geneva-rsl-to-file-config.png`)

  Placeholder: Configure Job Fields for Geneva RSL to File.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
