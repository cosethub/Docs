import { Callout } from 'nextra/components'

# ETL Job

Extract, transform, and load data between systems.

| | |
| --- | --- |
| **Category** | Database and Reporting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Configure source and target.
2. Map transforms.
3. Schedule or trigger from a parent.

<Callout type="info">
  **Screenshot placeholder** (`job-etl-job-picker.png`)

  Placeholder: Job type picker with ETL Job selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-etl-job-config.png`)

  Placeholder: Configure Job Fields for ETL Job.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
