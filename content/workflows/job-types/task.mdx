import { Callout } from 'nextra/components'

# Task

Pause the workflow until a person approves (or rejects) continuing.

> Generic approval gate. For data-focused review, prefer Data Review Task.

| | |
| --- | --- |
| **Category** | Email and Communication |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Assign users/groups.
2. Write instructions.
3. Child jobs use On success / On failure based on the response.

<Callout type="info">
  **Screenshot placeholder** (`job-task-picker.png`)

  Placeholder: Job type picker with Task selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-task-config.png`)

  Placeholder: Configure Job Fields for Task.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
