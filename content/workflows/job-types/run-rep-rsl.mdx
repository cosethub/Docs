import { Callout } from 'nextra/components'

# Run Rep RSL

Run a Runrep RSL and save output for regulatory or client reporting.

| | |
| --- | --- |
| **Category** | Finance and Accounting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Select RSL.
2. Set output directory.
3. Longer default duration than simple RSL jobs.

<Callout type="info">
  **Screenshot placeholder** (`job-run-rep-rsl-picker.png`)

  Placeholder: Job type picker with Run Rep RSL selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-run-rep-rsl-config.png`)

  Placeholder: Configure Job Fields for Run Rep RSL.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
