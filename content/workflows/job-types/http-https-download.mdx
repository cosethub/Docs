import { Callout } from 'nextra/components'

# HTTP/HTTPs Download

Download files from a web URL over HTTP or HTTPS.

| | |
| --- | --- |
| **Category** | Email and Communication |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Enter URL and auth if needed.
2. Choose destination folder/file.
3. Chain into file processing jobs.

<Callout type="info">
  **Screenshot placeholder** (`job-http-https-download-picker.png`)

  Placeholder: Job type picker with HTTP/HTTPs Download selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-http-https-download-config.png`)

  Placeholder: Configure Job Fields for HTTP/HTTPs Download.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
