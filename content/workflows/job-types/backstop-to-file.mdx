import { Callout } from 'nextra/components'

# Backstop to File

Run Backstop reports/returns and export to files.

| | |
| --- | --- |
| **Category** | Finance and Accounting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Select Backstop credentials and report.
2. Choose output folder.
3. Process or distribute the file.

<Callout type="info">
  **Screenshot placeholder** (`job-backstop-to-file-picker.png`)

  Placeholder: Job type picker with Backstop to File selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-backstop-to-file-config.png`)

  Placeholder: Configure Job Fields for Backstop to File.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
