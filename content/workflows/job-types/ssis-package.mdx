import { Callout } from 'nextra/components'

# SSIS Package

Execute an SSIS package, with optional parameters.

| | |
| --- | --- |
| **Category** | Database and Reporting |
| **Where you pick it** | Step 1: Choose Your Job Type |

## How it works

1. Select package and parameters.
2. Run on the appropriate host.
3. Chain reporting or loads after success.

<Callout type="info">
  **Screenshot placeholder** (`job-ssis-package-picker.png`)

  Placeholder: Job type picker with SSIS Package selected.
</Callout>

<Callout type="info">
  **Screenshot placeholder** (`job-ssis-package-config.png`)

  Placeholder: Configure Job Fields for SSIS Package.
</Callout>

## In a workflow tree

- Add as a child under a **Group** (or under another job).
- Give it a schedule, or **Run job based on parent status** (**On success** / **On failure** / **Always**).
- Add children for the next step after this job finishes.

See [Configure workflow trees](../workflow-trees) and [Examples](../examples).
