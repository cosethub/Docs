# SSRS Server Address

Stores the SSRS server URL used by reporting configuration.

| | |
| --- | --- |
| **Group** | Database |
| **Where you create it** | Credential Hub → Create Credential |
| **Commonly used by** | SSRS / Reporting configuration |

## How it works

1. Enter SSRS Server URL.
2. Use from Reporting / SSRS setup that needs a server address credential.

## Connection Settings fields

- **SSRS Server URL**

{/* SCREENSHOT: SSRS Server Address type in Create Credential */}

{/* SCREENSHOT: SSRS Server Address Connection Settings */}

## In a job

Jobs that need this type show a credential dropdown (for example **FTP Server Credentials** or **Credential**). You can also use **Add New … Credential** from some job forms.

See [Getting started](/credentials/getting-started) and [Examples](/credentials/examples).
